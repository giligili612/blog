{
  "private": true,
  "scripts": {
    "build": "PATH=$PWD/node_modules/sass-embedded-linux-x64/dart-sass:$PATH hugo --minify --cleanDestinationDir",
    "dev": "PATH=$PWD/node_modules/sass-embedded-linux-x64/dart-sass:$PATH hugo server -D"
  },
  "devDependencies": {
    "sass-embedded": "^1.100.0"
  }
}
